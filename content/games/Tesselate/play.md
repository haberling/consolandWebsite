# Play

Same game, in the browser. Click the terminal to focus.

```wasmterm
app: !url "content/games/tesselate-wasm"
cols: 80
rows: 32
fontSize: 24
```

First load pulls a large soundfont, so give it a second.
